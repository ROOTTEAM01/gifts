<link href="extension/wbcountdown/catalog/view/countdown/jquery.countdown.css" rel="stylesheet" type="text/css" />
<script src="extension/wbcountdown/catalog/view/countdown/jquery.plugin.min.js" type="text/javascript"></script>
<script src="extension/wbcountdown/catalog/view/countdown/jquery.countdown.min.js" type="text/javascript"></script>
  {# <h2 class="heading">
  <span>{{ heading_title }}</span>
  <a href="{{ special }}" class="spebtm">{{ product_btn }}</a>
</h2> #}
<div class="col-md-9 w-78 p-0">
<div class="wbcount">
  {% for product in products %}
  <form method="post" data-oc-toggle="ajax" data-oc-load="{{ cart }}" data-oc-target="#header-cart">
  <div class="product-layout">
    <div class="product-thumb transition">
      <div class="image">
        <!-- Save Price start -->
        {% if product.price %}
           {% if product.special %}            
             <span class="you-save"> Save {{ product.save }}</span> 
          {% endif %}
        {% endif %}
        <!-- Save Price end -->
      <a href="{{ product.href }}">
      <img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-fluid" /></a>
      <div id="Countdown{{ product.product_id }}" class="box-timer"></div>
        {# <div id="Countdown42" class="box-timer is-countdown"><span class="countdown-row countdown-show4 "><span class="countdown-section"><span class="countdown-amount">758</span><span class="countdown-period">Days</span></span><span class="countdown-section"><span class="countdown-amount">8</span><span class="countdown-period">Hrs</span></span><span class="countdown-section"><span class="countdown-amount">21</span><span class="countdown-period">Mins</span></span><span class="countdown-section"><span class="countdown-amount">30</span><span class="countdown-period">Secs</span></span></span></div>  #}        
      </div>
      <div class="caption">
              <!-- discount % start -->
        {% if product.price %}        
          {% if product.special %}
            <span class="discount-onprice">{{ product.discount }}%</span>                     
          {% endif %}
        {% endif %}
        <!-- discount % end -->          
            {# <div class="bran">
              <span><a href="{{ product.manufacturer_id }}" class="textdeb">{{ product.manufacturer }}</a></span>
            </div> #}
        <h4 class="protitle"><a href="{{ product.href }}">{{ product.name }}</a></h4>
         <div class="rating">
            {% for i in 1..5 %}
                {% if product.rating < i %} 
                <span class="fa-stack"><i class="fa-regular fa-star fa-stack-1x"></i></span>
              {% else %}
                <span class="fa-stack"><i class="fa-solid fa-star fa-stack-1x"></i><i class="fa-regular fa-star fa-stack-1x"></i></span>
              {% endif %}
              {% endfor %}         
              <span class="rate-tot">({{ product.tab_review }})</span>
          </div>
        {# <p>{{ product.description }}</p> #}
        <div class="pro-varient">        
        
         {# <div class="homeqt">
             <div class="homeqtinner">
              <button type="button" onclick="qty.minus('{{ product_id }}')" class="form-control pull-left btn-number btnminus dis-{{ product_id }}" disabled="disabled">
              <span class="fa fa-minus"></span>
            </button>
            <input name="quantity" class="addhqty" type="text" value="1" size="2" min="1" max="999" id="input-quantity-{{ product_id }}" class="form-control input-number pull-left" />
            <button type="button" onclick="qty.plus('{{ product_id }}')" class="form-control pull-left btn-number btnplus">
              <span class="fa fa-plus"></span>
            </button>
            </div>
          </div> #}
        </div>
        {% if product.price %}
        <p class="price"> {% if not product.special %}
          {{ product.price }}
          {% else %} <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span> {% endif %}
          {# {% if product.tax %} <span class="price-tax">{{ text_tax }} {{ product.tax }}</span> {% endif %} #} </p>
        {% endif %} 
          {# <span class="countdec">{{ count_desc }}</span> #}
              <div class="homeadd d-flex justify-content-between align-items-center">
        <div class="wrap input-group">
          <button type="button" id="sub" class="sub btn">-</button>
          <input name="quantity" class="count form-control" type="text" id="1" value="1" min="1" />
          <button type="button" id="add" class="add btn">+</button>
        </div>
        <button type="submit" formaction="{{ add_to_cart }}" data-bs-toggle="tooltip" title="{{ button_cart }}" class="cartb"><span>{{ button_cart }}</span> <i class="fa fa-angle-right" aria-hidden="true"></i></button>
      </div>     
      {# <div class="button-group">
        <button type="submit" formaction="{{ add_to_wishlist }}" data-bs-toggle="tooltip" title="{{ button_wishlist }}"><svg><use xlink:href="#heart"></use></svg></button>
        <button type="submit" formaction="{{ add_to_compare }}" data-bs-toggle="tooltip" title="{{ button_compare }}"><svg><use xlink:href="#compare"></use></svg></button>
      </div> #}
      </div>
      <input type="hidden" name="product_id" value="{{ product.product_id }}"/>
    <input type="hidden" name="quantity" value="{{ product.minimum }}"/>
    </div>
  

   {% if product.date_end %}
            <script type="text/javascript">
                $(document).ready(function () {
                    $('#Countdown{{ product.product_id }}').countdown({
                        until: new Date({{ product.date_end|date("Y") }}, {{ product.date_end|date("m") }}-1, {{ product.date_end|date("d") }}, {{ product.date_end|date("H") }}, {{ product.date_end|date("i") }}, {{ product.date_end|date("s") }}),
                        labels: ['Years', 'Months', 'Weeks', 'Days', 'Hrs', 'Mins', 'Secs'],
                        labels1: ['Year', 'Month', 'Week', 'Day', 'Hr', 'Min', 'Sec'],
                    });
                    //$('#Countdown<?php echo $product['product_id'];?>').countdown('pause');
                });
            </script>
            {% endif %}
            </div>
            </form>
  {% endfor %}

 </div>
</div>
</div>
</div>
</div>
</div>